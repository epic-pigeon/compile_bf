Compile with the best optimization using

`
java Main.class optimize !run input.bf output.class
`

Interpret with

`
java Main.class !optimize run input.bf
`

You can also enable methods (`java Main.class methods !optimize run input.bf`) to improve performance in some programs.